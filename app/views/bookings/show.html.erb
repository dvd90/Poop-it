<% if policy(Booking).show? %>
<h1>Booking confirmed!</h1>
<div class="container">
  <div id="map" style="width: 100%; height: 800px; "data-markers="<%= @markers.to_json %>">
</div>
<%= @booking.review %>
<% end %>
